source 'https://rubygems.org'


# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.6'
# Use mysql as the database for Active Record
gem 'mysql2', '0.3.18'
# Use SCSS for stylesheets
 gem 'sass-rails', '~> 4.0.2'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '~> 4.1.0'
# See https://github.com/rails/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Use jquery as the JavaScript library
gem 'jquery-rails'
# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'
# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0', group: :doc

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Use Unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano-rails', group: :development

# This is a small gem which causes rails console to open pry
gem 'pry-rails'
gem 'compass-rails','~> 2.0'
# management for JavaScript and CSS assets,as well as a powerful preprocessor pipeline
# that allows you to write assets in languages like CoffeeScript, Sass and SCSS.
gem 'sprockets', '2.11.0'
# A paginator for modern web app frameworks and ORMs
gem 'kaminari'
# Authentication for singin, Login ect.
gem 'devise'


group :development, :test do
  # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'byebug'
end

group :development do
  # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'web-console', '~> 2.0'
  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'spring'

end

group :test do
  # rspec-rails is a testing framework for Rails 3.x, 4.x and 5.x.
  # $ rails g rspec:install to use this gem. also, add "--format documentation"at'.rspec'file to run.
  gem 'rspec-rails'
  # a fixtures replacement with a straightforward definition syntax, support for multiple build strategies,and support for multiple factories for the same class
  gem 'factory_girl_rails', "~> 4.4.1"
  # This gem brings back assigns to your controller tests as well as assert_template to both controller and integration tests.
  gem 'rails-controller-testing'
  # This gem is a port of Perl's Data::Faker library that generates fake data.
  gem 'faker'
  gem 'capybara'

end
